apiVersion: v1
kind: Service
metadata:
  name: ride-matching-service
spec:
  type: LoadBalancer
  selector:
    app: ride-matching
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
